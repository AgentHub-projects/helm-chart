agenthub-gateway has been deployed.

Service:
  {{ include "agenthub-gateway.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local

IngressRoute:
  Uses Traefik entryPoint {{ .Values.ingressRoute.entryPoint | default "web" }}.
  Gateway routes: /socket.io, /filesystem.
  Fullstack routes: /api -> backend, / -> frontend.

Config:
  /etc/agenthub-gateway/config.yaml
